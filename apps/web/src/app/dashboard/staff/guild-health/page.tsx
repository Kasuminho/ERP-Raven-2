'use client';
import Link from 'next/link'; import { Badge } from '@/components/ui/badge'; import { Card,CardContent,CardHeader,CardTitle } from '@/components/ui/card'; import { useGuildHealthSignals } from '@/hooks/use-guild-health-api';
export default function GuildHealthPage(){const workspace=useGuildHealthSignals();return <div className="space-y-6"><div><p className="text-sm uppercase text-primary">Saude operacional explicavel</p><h1 className="font-[var(--font-cinzel)] text-3xl font-bold">Sinais para conversa, nunca score</h1><p className="mt-2 text-sm text-muted-foreground">Cada card mostra fatos e janela. Nao existe score de churn/lealdade, remocao, bloqueio ou efeito automatico em loot.</p></div><div className="grid gap-3 sm:grid-cols-4"><Card><CardContent className="p-4"><p className="text-xs uppercase text-muted-foreground">Sinais atuais</p><p className="text-2xl font-bold">{workspace.data?.signalCount??0}</p></CardContent></Card><Card><CardContent className="p-4"><p className="text-xs uppercase text-muted-foreground">Score unico</p><p className="font-semibold">Nao existe</p></CardContent></Card><Card><CardContent className="p-4"><p className="text-xs uppercase text-muted-foreground">Acao automatica</p><p className="font-semibold">Nenhuma</p></CardContent></Card><Card><CardContent className="p-4"><p className="text-xs uppercase text-muted-foreground">Efeito em loot</p><p className="font-semibold">Nenhum</p></CardContent></Card></div>{workspace.data?.signals.map(signal=><Card key={signal.key}><CardHeader><CardTitle className="flex flex-wrap items-center gap-2">{signal.subjectLabel}<Badge tone="blue">{signal.kind}</Badge><Badge tone="gold">{signal.window.label}</Badge></CardTitle></CardHeader><CardContent className="space-y-3"><p className="text-sm">{signal.explanation}</p><div className="flex flex-wrap gap-2">{signal.facts.map(f=><span key={f.label} className="rounded border border-white/10 px-2 py-1 text-xs">{f.label}: {f.value}</span>)}</div><p className="text-sm text-muted-foreground">Proxima conversa: {signal.recommendedAction}</p><Link className="text-sm font-semibold text-primary" href={signal.href}>Abrir evidencia</Link></CardContent></Card>)}</div>}
